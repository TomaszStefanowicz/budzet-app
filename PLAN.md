# PLAN.md — plan realizacji

**Termin:** 14 dni. **Deadline:** 2026-08-23.

**[UZUPEŁNIENIE 2026-08-27]** Kurs zaliczony (projekt oddany 2026-08-21, oceniony pozytywnie). Etapy 0–4 zamknięte i pozostawione poniżej bez zmian jako zapis historyczny; prace po zaliczeniu zaczynają się od **Etapu 5** (rozdział środowisk — blokuje wszystko inne).

Zadania odhaczamy w trakcie pracy (`[x]`). Każda sesja z Claude Code zaczyna się od: *„przeczytaj PLAN.md, realizujemy zadanie X"*.

**Zasada nadrzędna:** kończymy etap dopiero wtedy, gdy jego kryterium ukończenia jest spełnione i zmiany są w `main` (czyli na produkcji). Nie przechodzimy dalej z „prawie działającym" etapem — przy imporcie i zestawieniach dług techniczny mści się natychmiast.

---

## Etap 0 — Fundament (dni 1–2)

Cel: działający publiczny link i wszystko, co potrzebne, żeby dalej pisać już tylko logikę.

- [x] **0.1** Repozytorium na GitHubie, pierwszy commit z `CLAUDE.md`, `SPEC.md`, `PLAN.md`.
- [x] **0.2** Szkielet Next.js + TypeScript, konfiguracja Vitest, `.gitignore` z `.env.local`.
- [x] **0.3** Projekt Supabase, podpięcie do Vercela, zmienne środowiskowe.
**[UZUPEŁNIENIE] Zadanie 0.4 rozbite na mniejsze kroki (podejście „chodzący szkielet")** — zamiast jednej migracji ze wszystkimi tabelami z `SPEC.md` IV.5 na raz, budujemy jedną cienką, ale kompletną ścieżkę (baza → logowanie → ekran podglądu) i dopiero sprawdzoną rozszerzamy. Pozostałe tabele (`clients`, `revenue_items`, `revenue_months`) powstają w Etapie 1, gdy import faktycznie ich potrzebuje; `report_archive` — w Etapie 4.

- [x] **0.4a** Migracja SQL: tabela `imports` (metryki importu — data, nazwa pliku, liczba wersów, wynik walidacji, zakres wykrytych miesięcy). Struktura bazy powstaje z pliku, nie z panelu.
- [x] **0.5** Deploy na Vercel z gałęzi `main`. **Publiczny link musi działać.** → https://budzet-app-sigma.vercel.app
- [x] **0.6** Supabase Auth: strona logowania, middleware blokujący wszystkie pozostałe ścieżki, rejestracja samodzielna wyłączona. Konto właściciela.
- [x] **0.4b** Prosty ekran podglądu (po zalogowaniu) czytający z tabeli `imports` — potwierdzenie, że przeglądarka → API → baza działają razem, zanim dołożymy kolejne tabele.
- [x] **0.7** **Generator danych syntetycznych** (`scripts/generate-synthetic-data.ts`) produkujący plik `.xlsx` zgodny z `SPEC.md` II — dane oczywiście fikcyjne, z wszystkimi przypadkami brzegowymi z `SPEC.md` VI.9. Dwa tryby: `npm run generate:data` (czyste dane, do demo) i `npm run generate:data -- --with-errors` (golden file z celowymi błędami, do testów). Wyniki w `test-data/`.
- [x] **0.8** Plik testowy z prawdziwej struktury: rzeczywisty wycinek danych, z podmienionymi danymi 3 klientów objętych NDA (`SPEC.md` V.25) — **do użytku wyłącznie lokalnego**, w `local-data/` (gitignored), nigdy commitowany. Potrzebny do backtestu w Etapie 2. → `local-data/Sprzedaz.xlsx`

**Kryterium ukończenia:** wchodzę na publiczny link, loguję się, widzę ekran podglądu importów (na razie pusty). `npm test` przechodzi. Mam plik syntetyczny i plik do backtestu.

---

## Etap 1 — Import i walidacja (dni 3–5)

Cel: serce aplikacji. To tu kryje się największe ryzyko z decyzji V.5 — „policzy błędnie, ale wiarygodnie".

**[UZUPEŁNIENIE] Kolejność zadań — chodzący szkielet, potem warstwy walidacji.** Zamiast całej logiki najpierw i ekranu na końcu (jak w pierwotnym rozpisaniu), najpierw budujemy minimalny, ale kompletny szkielet uploadu (1.1) — testowalny ręcznie od razu plikiem syntetycznym — a dopiero na nim dokładamy kolejne warstwy walidacji, każdą z automatycznymi testami i widoczną natychmiast na ekranie.

- [x] **1.0** Migracja SQL: tabele `clients`, `revenue_items`, `revenue_months` (`SPEC.md` IV.5) — dokładane teraz, bo import ich potrzebuje.
- [x] **1.1** Minimalny endpoint API + ekran uploadu: przyjmuje plik `.xlsx`, parsuje strukturalnie (SheetJS) — odczyt kolumn A–I, **dynamiczne wykrywanie zakresu kolumn miesięcznych** (`SPEC.md` II.2, II.4) — **bez walidacji reguł biznesowych na tym etapie**. Ekran pokazuje liczbę odczytanych wersów i wykryty zakres miesięcy. Parser jako czysta funkcja w `lib/`, bez dostępu do bazy. Testy. **Testowalne ręcznie od razu.** → zweryfikowane ręcznie na obu plikach syntetycznych (1127 / 1134 wersów, zakres 2024/01–2027/12).
- [x] **1.2** Walidacja struktury: układ kolumn, format numeru dokumentu `TYP/rrrr/mm/nnnn`, format kwot, puste komórki miesięcy jako 0. Testy do każdej reguły. Błędy widoczne na ekranie uploadu. → zweryfikowane ręcznie lokalnie: plik syntetyczny czysty przechodzi bez błędów, plik z celowymi błędami odrzucony w całości z czytelnym raportem (numer wersu + przyczyna). Doprecyzowania zakresu (flaga I, wartość flagi =1, ciągłość lp, format NIP/VAT UE) zapisane w `SPEC.md` V.25.
- [x] **1.3** Walidacja typów dokumentów (`SPEC.md` II.3.h) i reguł flag (II.3.a–f): dokładnie jedna flaga, FKS bez flag, FVZK tylko H lub I, FKS z wartościami ujemnymi. Testy. → zweryfikowane ręcznie lokalnie: plik syntetyczny czysty przechodzi bez błędów, plik z celowymi błędami wykrywa dodatkowo nieznany typ dokumentu (NIP 1000000087). Reguła długości pakietu (F/G ≥ 2 miesiące, I dokładnie 1 miesiąc, H bez ograniczeń) zaimplementowana i opisana w `SPEC.md` V.27. Powroty po 11/13 miesiącach z błędną flagą — poza zakresem, to zadanie 1.4.
- [x] **1.4** Walidacja spójności flag F/G — okno 12 miesięcy per NIP z regułą horyzontu (`SPEC.md` II.3.g). **Najtrudniejsza reguła w projekcie.** Osobna sesja, testy dla obu stron granicy: powrót po 11 miesiącach (błąd) i po 13 (poprawne), oraz flaga G na początku horyzontu (poprawne). → zweryfikowane ręcznie lokalnie: plik czysty przechodzi bez błędów, plik z błędami wykrywa oba błędne powroty (11 mies. jako F, 13 mies. jako G). Flaga I (i H) wykluczona z łańcucha ciągłości dostępu — potwierdzone empirycznie i opisane w `SPEC.md` V.28. Przy tej analizie znaleziono i naprawiono błąd w `scripts/generate-synthetic-data.ts` (patrz dziennik poniżej), oba pliki syntetyczne przegenerowane.
- [x] **1.5** Ujednolicenie raportu błędów: wszystkie problemy jednocześnie, każdy z numerem wersu i przyczyną, komunikaty po polsku (`SPEC.md` VI.4). → rozszerzone po weryfikacji na rzeczywistym pliku: numeracja pozycji, sortowanie po wersie, identyfikacja wersu (lp/NIP/klient), czytelne etykiety flag F/G/H/I zamiast gołych liter (`SPEC.md` V.32).
- [x] **1.6** Zapis do bazy: import całościowy zastępujący stan, metryki importu, aktualizacja słownika klientów (nowi klienci z typem „nieokreślony"). Rozbite na mniejsze kroki:
  - [x] **1.6a** `parseSalesRows` wylicza `sale_month` z numeru dokumentu (wyłącznie `rrrr/mm`, niezależnie od kolumn miesięcznych — `SPEC.md` II.2). Przy okazji doprecyzowano `SPEC.md` V.33 (korekty FKS nie mogą retroaktywnie zmieniać już zrealizowanych miesięcy) i naprawiono generator danych syntetycznych.
  - [x] **1.6b** Słownik klientów: nowy klient (NIP nieznany) → wstawiany z typem „nieokreślony"; istniejący klient z inną nazwą w pliku → nazwa aktualizowana, stara zachowana w `previous_name` (`SPEC.md` V.35); typ zawsze bez zmian. Przy okazji zaostrzono format NIP (`SPEC.md` V.34, koryguje V.25.d) — myślniki/spacje to teraz błąd, nie coś do zignorowania.
  - [x] **1.6c** Zapis metryk importu do `imports`, dla sukcesu i błędu walidacji (bez rozszerzania schematu — sama lista błędów nie jest zapisywana, tylko wynik; do rozważenia później, jeśli błędnych importów okaże się dużo).
  - [x] **1.6d** Zapis `revenue_items` + `revenue_months`: poprzednie pozycje `is_active = false` **przed** wstawieniem nowych (bez transakcji — `supabase-js` jej wprost nie daje; kolejność operacji ustalona z użytkownikiem tak, żeby błąd w trakcie zapisu dawał ewidentnie zerowy wynik, nie cichy błąd liczbowy — `SPEC.md` V.36). `revenue_months` to dosłowny odczyt kolumn miesięcznych z pliku, bez żadnych obliczeń (`SPEC.md` V.33).
- [x] **1.7** Domknięcie ekranu uploadu: historia importów, pełny raport błędów, integracja z ekranem z zadania 0.4b. Test end-to-end: plik syntetyczny wchodzi, plik z błędami jest odrzucany z czytelnym raportem. → `UploadForm` odświeża historię importów (`router.refresh()`) natychmiast po każdej próbie, bez ręcznego przeładowania strony; ekran historii pokazuje też wykryty zakres miesięcy. Import poprawny/błędny/ponowny zweryfikowany ręcznie na produkcji w poprzedniej sesji (bez samego odświeżania, dodanego dopiero teraz) — samo odświeżanie do zweryfikowania po wdrożeniu.

**Kryterium ukończenia:** na produkcji importuję plik syntetyczny i widzę dane w panelu Supabase. Importuję plik z błędami i dostaję listę wszystkich błędów z numerami wersów. Import tego samego pliku dwukrotnie daje identyczny stan bazy.

---

## Etap 2 — Silnik zestawień (dni 6–8)

**[UZUPEŁNIENIE] Ograniczenie zakresu na czas terminu (decyzja `SPEC.md` V.37).** Przy 7 dniach kalendarzowych do terminu i wzorcu z Etapu 1 (prawie każde zadanie ujawniało coś wymagającego korekty po teście na danych rzeczywistych) świadomie odkładamy zestawienia 13–15 (2.4, 2.5) jako bufor czasowy — wracamy do nich, jeśli zostanie czas, inaczej po zaliczeniu. Priorytet realizacji: **2.1 → 2.2 → 2.3 → 2.6 → 2.7 → (jeśli zostanie czas) 2.4 → 2.5.**

- [x] **2.1** Warstwa agregacji per klient (`SPEC.md` 11a): miesięczne serie przychodów per NIP, z regułą „przychód > 0". Czyste funkcje (`parseDecimalToGrosze`, `aggregateMonthlyRevenuePerClient`) + testy. Filtr F/G/H+FKS (bez I) dla przyszłych zestawień 13–15 to jeden dodatkowy `.filter()` u wywołującego, nie osobna logika. Przy okazji: skrypt `scripts/preview-report.ts` do ręcznego podglądu wyników zanim powstanie ekran zestawień (Etap 3) — dwa tryby źródła danych: `--month=rrrr-mm` (baza demo, zweryfikowane: 89 klientów w 2024-06) i `--file=sciezka.xlsx --month=rrrr-mm` (czyta plik lokalnie, **bez dotykania bazy** — bezpieczne dla `local-data/Sprzedaz.xlsx`, zweryfikowane: 104 klientów w 2024-06 na realnych danych).
- [x] **2.2** Zestawienia 1–11 (`SPEC.md` III.A) + wyodrębniona pozycja „korekty" (decyzja V.10). Jedna funkcja `sumByFlag` obsługuje obie rodziny (2–6 wg miesiąca sprzedaży, 7–11 wg miesiąca kalendarzowego) — różni je tylko dane wejściowe. Test kontrolny (F+G+H+I+korekty=total) przechodzi w testach jednostkowych i na realnych danych (`preview-report.ts`, sprawdzone na `local-data/Sprzedaz.xlsx`: 485602.00 = 136120+348502+980+0+0 dla sprzedaży, 111994.17 = 36047.56+71784.34+6162.27+0−2000 dla przychodu). Przy okazji naprawione (zanim zdążyło trafić do builda) dwa kolejne tranzytywne importy bez rozszerzenia `.ts` w `buildRevenueItemRow.ts` — build sprawdzony PRZED commitem tym razem.
- [x] **2.3** Zestawienie 12 — liczba i lista klientów z przychodami, z sumą faktur i numerami dokumentów. `buildClientMonthlyRevenueReport` rozróżnia „przychód miesiąca" (tylko część kwoty przypadająca na dany miesiąc) od „sumy faktur" (pełna wartość każdej faktury dotykającej ten miesiąc, SPEC.md 12.b.v) — zweryfikowane na realnych danych (`local-data/Sprzedaz.xlsx`): np. 84700 zł faktury / 12 mies. = 7058.33 zł przychodu miesiąca, dokładnie się zgadza.
- [x] **2.6** Zestawienie 16 — banki i SKOK-i na podstawie słownika. `countBanksAndSkoks` — prosty filtr/licznik nad listą z zestawienia 12. Bez pełnego backtestu na danych rzeczywistych (świadoma decyzja z użytkownikiem) — typ klienta istnieje wyłącznie w słowniku aplikacji, nigdy w pliku, więc nie ma czego porównać z Excelem; ryzyko ocenione jako niskie (prosty filtr, brak logiki brzegowej). Tryb `--file=` skryptu podglądu pokazuje to jawnie jako niedostępne, zamiast mylącego zera. Na danych demo wynik „0" zweryfikowany bezpośrednio w bazie jako poprawny (wszyscy klienci mają dziś typ „nieokreślony" — edycja słownika to zadanie 3.2).
- [x] **2.7** **BACKTEST** (dla zestawień 1–12, 16), zamknięty w formie rozłożonej na zadania 2.2/2.3 zamiast jednorazowo na końcu — porównanie z Excelem robione na bieżąco po każdym zadaniu (2 miesiące z 2026, `local-data/Sprzedaz.xlsx`), zamiast czekać do jednego zbiorczego przebiegu. Jedyna znaleziona rozbieżność okazała się błędem w formule budżetu (sztywny wers po usunięciu wersów), nie w kodzie — patrz dziennik 2026-08-17. Trzeci miesiąc (z lat wcześniejszych) świadomie pominięty: wymagałby przeliczenia ręcznych zestawień referencyjnych na poprawionych danych (~30–40 min pracy tylko do porównania) za spodziewanie niską korzyść (drobne, już zdiagnozowane rozbieżności źródłowe, nie błędy aplikacji) — koszt przewyższa efekt. Zestawienie 16 poza zakresem backtestu (patrz 2.6 — nie da się go zweryfikować na danych rzeczywistych, typ klienta istnieje wyłącznie w słowniku aplikacji).
- [x] **2.4** Zestawienie 13 — klienci, których umowy wygasły w miesiącu i dotychczas nie przedłużyli (**definicja doprecyzowana, SPEC.md V.43** — nie tylko brak przychodu w M+1, ale w żadnym późniejszym miesiącu do końca danych), **z regułą horyzontu `SPEC.md` 13.d** i kwotą przychodów z miesiąca POPRZEDNIEGO (13.b.v). → Silnik (`buildExpiringContractsReport` + `shiftMonth` + `isWithinExpiringHorizon`, testy jednostkowe) podłączony do ekranu/eksportu/archiwum. Backtest na `local-data/Sprzedaz.xlsx` (V.41): marzec 2026 zgodny od razu (plik = baza = ekran). Maj 2026 ujawnił 2 klientów (Salumanus, Wschodni Bank Spółdzielczy w Chełmie) błędnie wykazanych jako "nadal wygaśli" — mieli realną, jednomiesięczną przerwę, ale już przedłużyli (z opóźnieniem/świadomą przerwą, rozliczenie wg daty dostępu z faktury, nie daty wystawienia). Naprawione zmianą definicji (V.43) — po poprawce obaj poprawnie zniknęli z listy dla maja, wciąż widoczni w zestawieniu 12 (mają przychód w maju). Podział na dwa osobne zestawienia (z przedłużeniem / bez) odłożony na przyszłość — P7.
- [x] **2.5** Zestawienia 14 i 15 — startujące umowy nowe i przedłużenia, **z regułą horyzontu doprecyzowaną decyzją V.44** (wymaga tylko M+1, nie M−1 — inaczej niż 13). → Wspólny silnik `buildPackageStartReport` (parametryzowany flagą F/G) + `isWithinPackageStartHorizon`, testy jednostkowe. Kwalifikacja po fladze wersu (najwcześniejszy miesiąc z przychodem), nie po agregacie M−1/M/M+1 — ta sama poprawka, co w 13 (V.43), ustalona z użytkownikiem **przed** kodowaniem, więc bez błędu do naprawy po fakcie. Wartość z M+1 (potwierdzone z użytkownikiem). Backtest na marcu i maju 2026 (`local-data/Sprzedaz.xlsx`): ekran = podgląd ze skryptu, zero rozbieżności. Jeden nietypowy przypadek zweryfikowany z użytkownikiem: klient z cykliczną miesięczną fakturą (MiŚOT S.A.) poprawnie pojawia się w 15 co miesiąc — to nie błąd, klient faktycznie przedłuża co miesiąc.

**Kryterium ukończenia (zakres minimalny na termin):** zestawienia 1–12 i 16 liczą się poprawnie, backtest dla nich zamknięty, rozbieżności wyjaśnione. Zestawienia 13–15 były celem rozszerzonym, nie warunkiem zaliczenia w terminie — **ostatecznie zrealizowane (2.4, 2.5) i zbacktestowane** przy pracy nad kompletem wskaźników, patrz dziennik 2026-08-17/18.

---

## Etap 3 — Interfejs (dni 9–11)

- [x] **3.1** Ekran importu: upload, historia importów z metrykami, czytelny raport błędów. → zrealizowane już w zadaniu 1.7, kryterium spełnione bez dodatkowej pracy.
- [x] **3.2** Słownik klientów: lista, edycja typu (bank / SKOK / inny), **wyróżnienie klientów z typem „nieokreślony"**. → `/clients`: tabela z licznikiem nieuzupełnionych typów, edycja przez `<select>` (server action `updateClientType`, `revalidatePath`), wiersze „nieokreślony" podświetlone. Zweryfikowane ręcznie lokalnie na współdzielonej bazie: zmiana zapisuje się i przeżywa odświeżenie strony, licznik się aktualizuje.
- [x] **3.3** Widoki zestawień: wybór miesiąca, tabele, listy klientów w zestawieniach 12–16. → `/reports`: wybór miesiąca z zakresu wykrytego w ostatnim udanym imporcie (domyślnie miesiąc bieżący, jeśli w zakresie, inaczej ostatni dostępny), tabela zestawień 1–11 + 16, lista zestawienia 12 (NIP, nazwa, przychód miesiąca, suma faktur, dokumenty). Nowa czysta funkcja `buildMonthlySummary` (1–11) z testami — reszta reużywa istniejące funkcje z Etapu 2 (`buildClientMonthlyRevenueReport`, `countBanksAndSkoks`). Zestawienia 13–15 poza zakresem (odłożone w 2.4/2.5). Zweryfikowane ręcznie lokalnie: domyślny miesiąc, przełączanie miesiąca (URL `?month=`), liczby niezerowe i zgodne z rzędem wielkości z wcześniejszego backtestu (2.7).
- [x] **3.4** Eksport zestawień do `.xlsx` (`SPEC.md` VI.5). → `GET /api/reports/export?month=rrrr-mm-01`: surowa tabela (decyzja V.39, rozstrzyga P5), dwa arkusze — „Zestawienia 1-11, 16" i „Zestawienie 12". Przycisk „Eksportuj do .xlsx" na `/reports`. Testy jednostkowe dla formatowania wierszy (grosze → złote, w tym korekty ujemne). Zweryfikowane ręcznie: endpoint zwraca 200, poprawny `Content-Type`, plik 89 KB dla 304 klientów.
- [x] **3.5** Nawigacja, komunikaty stanu, obsługa błędów w interfejsie. **[UZUPEŁNIONE 2026-08-18]** Zakres doprecyzowany po przeglądzie stanu aplikacji po 3.6–3.8 (część tematu już zrealizowana przy okazji tamtych zadań — stany ładowania/zapisywania w `UploadForm`, `ClientTypeSelect`, `ClientsTable`, `ArchiveButton`, obsługa błędów logowania):
  - [x] **3.5a** Wspólny komponent nawigacji (topbar) na `/`, `/reports`, `/clients` — dziś tylko strona główna ma pełny topbar (e-mail, linki, Wyloguj), pozostałe dwie mają wyłącznie "← Powrót do importu", więc przejście między Zestawieniami a Słownikiem wymaga powrotu na start. Podświetlenie aktywnej strony, Wyloguj dostępne wszędzie. **Szacowane: ~45 min–1h.** → Nowy `src/app/components/Nav.tsx` (server component, linki + Wyloguj), podpięty na wszystkich trzech stronach z odpowiednim `active`. Na `/reports` rozbite na dwa wiersze (Nav + osobny wiersz z eksportem/archiwum/selektorem miesiąca), bo oba rzędy kontrolek nie mieszczą się sensownie w jednym.
  - [x] **3.5b** `loading.tsx` dla `/reports` i `/clients` (obie strony `force-dynamic`, dane z bazy przy każdym wejściu) — dziś brak wskaźnika ładowania przy zmianie miesiąca/wejściu na stronę. **Szacowane: ~20–30 min.** → Prosty placeholder "Ładowanie…" w karcie pasującej szerokością do docelowej strony (mechanizm wbudowany Next.js App Router, zero logiki).
  - [x] **3.5c** Globalny `error.tsx` — dziś brak, nieobsłużony wyjątek pokazałby domyślny, nieostylowany ekran Next.js po angielsku zamiast komunikatu po polsku z linkiem powrotu. **Szacowane: ~20–30 min.** → `src/app/error.tsx` (client component wymagany przez Next.js), przycisk "Spróbuj ponownie" (`reset()`) + link do strony głównej.

  **Razem: ~1.5–2h szacowane, zrealizowane bez niespodzianek. `npm run build`/`npm test` (167/167) przechodzą. Nie zweryfikowane wizualnie w przeglądarce — brak danych logowania w tej sesji.**

**[NOWE, 2026-08-18] Usprawnienia UX/wizualne z `DESIGN_SPEC.md`.** Priorytety i wycena ustalone wspólnie z użytkownikiem po analizie kodu (nie tylko dokumentu) — kolejność wg stosunku kosztu do korzyści. Okno czasowe: pierwotnie do 2026-08-21 14:00 (rezerwa 48h przed terminem 2026-08-23 23:59 na 4.2a/4.3/4.6/4.7). **[ZAKTUALIZOWANE 2026-08-19]** Rzeczywisty czas realizacji 3.6–3.9 wyraźnie poniżej szacunków (patrz dziennik) — użytkownik wydłużył okno na dalsze poprawki/funkcje do końca czwartku (2026-08-20), a bufor na 4.2a/4.3/4.6/4.7 powiększył do piątku–niedzieli (2026-08-21 – 2026-08-23 23:59, ok. 3 dni zamiast 48h). Czas szacowany analizowany po każdym zadaniu w dzienniku poniżej.

- [x] **3.6** [`DESIGN_SPEC.md` P0] Tokeny wizualne globalne: hierarchia nagłówków sekcji, plakietki (badge) statusu sukces/błąd, stylowanie inputu pliku spójne z przyciskiem "Wczytaj", wyrównanie liczb do prawej we wszystkich tabelach, redukcja nadmiarowego paddingu w kartach. Wyłącznie CSS/klasy Tailwind, bez zmian struktury komponentów. **Szacowane: ~1.5h.** → Dodano `StatusBadge`, obniżono wagę e-maila w topbarze, wzmocniono nagłówki (`text-xl font-bold`), wyrównanie do prawej kwot/liczb, zebra (`even:bg-gray-50`), sticky header + scroll (`max-h-*vh overflow-y-auto`) dla długich list (historia importów, słownik, `ClientReportTable`), stylowany input pliku (`file:` klasy Tailwind). `npm run build` i `npm test` (167/167) przechodzą. Nie zweryfikowane wizualnie w przeglądarce w tej sesji — brak danych logowania; do potwierdzenia przez użytkownika na dev/produkcji.
- [x] **3.7** [`DESIGN_SPEC.md` P1, priorytet wysoki] Słownik klientów (`/clients`): wyszukiwanie substringowe po nazwie/NIP (`includes()`), sortowanie kolumn NIP/Nazwa/Typ, zaznaczanie zbiorcze (checkbox + "zaznacz wszystkie" uwzględniające filtr) + akcja "ustaw typ dla zaznaczonych" (nowa server action, `update().in('nip', [...])`, jedno zapytanie). Wymaga wydzielenia nowego client component z listy klientów. Utrzymać podświetlenie "nieokreślony". **Szacowane: ~2.5h.** → Nowy `ClientsTable.tsx` (client component): filtr `useMemo` nad propsami (bez kopiowania listy do lokalnego stanu, żeby dane odświeżały się po `revalidatePath` z bulk-akcji), sortowanie `localeCompare("pl")` po kliknięciu nagłówka, zaznaczanie ograniczone do aktualnie widocznych (przefiltrowanych) wierszy. Nowa server action `updateClientTypeBulk` w `actions.ts`. Zaimplementowane szybciej niż szacowano (bliżej ~1.5h) dzięki reużyciu istniejącego `ClientTypeSelect` do edycji pojedynczej. `npm run build`/`npm test` (167/167) przechodzą. Nie zweryfikowane wizualnie w przeglądarce — brak danych logowania w tej sesji.
- [x] **3.8** [`DESIGN_SPEC.md` P1] Selektor okresu: rok (dropdown) + miesiąc (dropdown/przyciski) zamiast jednej długiej listy `rrrr/mm`. Wyszukiwanie/sortowanie we współdzielonym `ClientReportTable` (już współdzielony między raportami 12–15, więc zmiana w jednym pliku). **Szacowane: ~1.5–2h.** → `MonthSelect` rozbity na dwa selecty (rok + miesiąc dostępny w danym roku), zmiana roku wybiera ten sam miesiąc jeśli istnieje w danych, inaczej pierwszy dostępny. `ClientReportTable` przerobiony na client component z wyszukiwaniem substringowym (NIP/nazwa) i sortowaniem po każdej kolumnie — przy okazji `clientNames` zmienione z `Map` na zwykły obiekt (`Object.fromEntries` w `page.tsx`), żeby uniknąć niepewności serializacji `Map` przez granicę serwer/klient RSC. Bliżej dolnej granicy estymacji (~1.5h) dzięki reużyciu wzorca z 3.7. `npm run build`/`npm test` (167/167) przechodzą. Nie zweryfikowane wizualnie w przeglądarce — brak danych logowania w tej sesji.
- [x] **3.9** [`DESIGN_SPEC.md` P1, opcjonalne — tylko jeśli zostanie czas w oknie do 21.08] Kolumna "Dokumenty" w `ClientReportTable`: liczba dokumentów + rozwinięcie pełnej listy na hover/klik zamiast wypisywania inline. **Szacowane: ~0.5–1h.** → Natywny `<details>/<summary>` (dostępność i klik/klawiatura za darmo, bez własnego stanu) — pojedynczy dokument wypisany wprost, 2+ pokazuje "N dokumentów" z rozwinięciem pełnej listy po kliknięciu. Szybciej niż szacowano (~15 min). `npm run build`/`npm test` (167/167) przechodzą.

**Odłożone poza ten sprint (decyzja 2026-08-18, za `DESIGN_SPEC.md`):** nawigacja zakładkowa/boczne menu między 16 raportami i zwijane sekcje (accordion) dla długich list (P2 w `DESIGN_SPEC.md`) — dotykają struktury komponentów/routingu, realne ryzyko regresji przy napiętym terminie. Do `PLAN.md` po zaliczeniu, jeśli będzie potrzeba. Przeprojektowanie ekranu startowego na dashboard i eksport w innym formacie niż surowa tabela — jak ustalono wcześniej, poza zakresem MVP.

**Kryterium ukończenia:** przechodzę pełną ścieżkę w przeglądarce na produkcji: logowanie → import → uzupełnienie słownika → przegląd zestawień → eksport.

---

## Etap 4 — Domknięcie i bufor (dni 12–14)

- [x] **4.1** Archiwum wygenerowanych zestawień (`SPEC.md` IV.3), w tym migracja SQL dla tabeli `report_archive`. → migracja `20260817140000` (kolumny: `import_id`, `month`, `generated_at`, `payload` jsonb). Przycisk „Zapisz do archiwum" na `/reports` (server action `archiveReport`) zapisuje migawkę (zestawienia 1–11+16 i lista 12) powiązaną z ostatnim udanym importem. Zweryfikowane ręcznie: zapis, potwierdzenie na ekranie, zawartość wiersza w bazie zgodna z ekranem. **[ZMIENIONE 2026-08-19]** Przeglądanie archiwum, pierwotnie świadomie poza zakresem, jednak zrealizowane — patrz zadanie niżej.
- [x] **4.1a** [NOWE 2026-08-19] Przeglądarka archiwum (`/archive`) — widok identyczny z `/reports` (wspólne komponenty `SummaryTable`/`ClientReportTable`/`ReportSection`), zamiast wyboru roku/miesiąca rozwijana lista migawek, zamiast przycisku „Zapisz do archiwum" czerwony napis „ARCHIWUM". Przycisk eksportu do `.xlsx` świadomie pominięty — działałby na bieżących danych, nie na zawartości migawki, co byłoby mylące. Nowa czysta funkcja `archivedPayload.ts` (z testami) odtwarza grosze ze złotówek zapisanych w `payload` (`parseDecimalToGrosze`, odwrotność `toClientPayload`/`breakdownToZl` z `actions.ts`) i defensywnie obsługuje różne kształty migawek w czasie — starsze migawki (sprzed V.45/V.46) nie mają `banksAndSkoksClients`, niektóre nie mają `expiringClients`/`newClients`/`renewalStarts` (miesiąc poza horyzontem w chwili archiwizacji) — brakujące sekcje pokazują komunikat "niedostępne w tej migawce" zamiast błędu.
- [x] **4.2** Instancja demo: zasilenie danymi syntetycznymi, uzupełniony słownik, wygenerowane zestawienia — organizator po zalogowaniu widzi działający produkt, nie pustą bazę. → Dane syntetyczne już były zaimportowane (Etap 1/2). Uzupełniono tylko słownik (500 klientów: 50 bank, 25 SKOK, 325 inny, 100 nieokreślony — proporcja demonstracyjna, nie biznesowa), operacja bezpośrednio na bazie (dane syntetyczne, odwracalne, za zgodą użytkownika). Zestawienie 16 dla marca 2026 pokazuje teraz 46 (zamiast 0) — zweryfikowane ręcznie.
- [x] **4.2a** [NOWE, patrz `SPEC.md` V.41] Domknięcie okna testów na danych rzeczywistych: `TRUNCATE` **pięciu** tabel (`imports`, `clients`, `revenue_items`, `revenue_months`, `report_archive` — łatwo zapomnieć o tej ostatniej) i reimport/dopracowanie danych syntetycznych dla oceniających (redo 4.2 na czysto). **Musi nastąpić przed 4.3** (zanim ktoś poza właścicielem projektu dostanie dostęp). → Użytkownik wykonał `TRUNCATE ... RESTART IDENTITY CASCADE` (SQL Editor) i reimport `test-data/dane-syntetyczne-clean.xlsx` (plik deterministyczny, ten sam co poprzednio) przez UI produkcyjne. Zweryfikowano skryptem przez `service_role`: 1 import, 500 klientów, wszyscy `nieokreślony`. Rozkład typów odtworzony jednorazowym skryptem (nie commitowany, jak w 4.2): 50 bank / 25 SKOK / 325 inny / 100 nieokreślony — zweryfikowane, rozkład końcowy zgodny.
- [x] **4.3** [ZMIENIONE 2026-08-21] Konto dla organizatorów kursu. **Zależy od 4.2a** — nie zakładać przed wyczyszczeniem danych rzeczywistych. → **Nieaktualne:** finalna dokumentacja szkolenia nie wymaga osobnego konta. Wystarczy podać w prezentacji link do aplikacji, login i hasło do istniejącego konta właściciela oraz link do publicznego repozytorium.
- [x] **4.4** `README.md`: co to jest, link do aplikacji, dane logowania demo, stack, jak uruchomić lokalnie, uwaga o danych syntetycznych. → **[ZMIENIONE 2026-08-21]** Zgodnie z rozstrzygnięciem 4.3: login/hasło do konta demo **nie** trafiają do publicznego README — przekazywane osobno, w prezentacji dla organizatorów. Sekcja „Dane logowania" wyjaśnia to zamiast podawać dane.
- [x] **4.5** [ODŁOŻONE NA KONIEC] Retrospektywa procesu tworzenia oprogramowania — nie jest wymagana do zaliczenia, to podsumowanie dla użytkownika (jak wyglądała praca z Claude Code, co wynikło ze specyfikacji, jakie decyzje zmieniły się w trakcie). Materiał już częściowo istnieje w dzienniku poniżej i w rejestrze decyzji `SPEC.md` V — do spisania na końcu prac. → Spisana w nowym pliku `RETROSPEKTYWA.md`: specyfikacja jako źródło prawdy i to, co z niej wynikło w konfrontacji z danymi rzeczywistymi; przebieg współpracy z Claude Code; trzy incydenty operacyjne i wnioski; zmiany zakresu względem planu pierwotnego; podsumowanie liczbowe.
- [x] **4.6** [ZMIENIONE 2026-08-21] Odpauzowanie Supabase i sprawdzenie linku **dzień przed** oddaniem (`SPEC.md` VI.7). **Przesunięte wcześniej:** oddanie pracy nie musi czekać do 23.08 — im szybciej sprawdzone, tym szybciej można wrócić do pracy na danych rzeczywistych. → Supabase aktywny (zapytanie serwisowe odpowiedziało błyskawicznie, bez typowego opóźnienia wybudzania z pauzy). Link produkcyjny sprawdzony w przeglądarce na zalogowanej sesji: Import (historia importu syntetycznego widoczna), Dane, Zestawienia (liczą się poprawnie dla 2026/08), Archiwum (poprawnie puste po czyszczeniu z 4.2a), Słownik klientów (rozkład typów zgodny z 4.2a) — wszystkie ekrany renderują się bez błędów. Jedyne komunikaty w konsoli to nieszkodliwy szum rozszerzenia przeglądarki, niezwiązany z aplikacją.
- [ ] **4.7** Bufor na poprawki. **Nienegocjowalny — coś zawsze się przesunie.**

---

## Etap 5 — Rozdział środowisk [NOWY 2026-08-27; patrz `SPEC.md` VI.3 i decyzje V.48–V.50]

Cel: opublikowany przez organizatora kursu link i hasło mają prowadzić do środowiska, w którym **nigdy** nie będzie ani danych rzeczywistych, ani pracy w toku. Rozdział wykonywany **przed** zgodą na publikację prezentacji, przed pierwszym commitem zmieniającym UI i przed załadowaniem danych rzeczywistych. Zadania oznaczone *(panel)* wykonuje właściciel ręcznie w panelach Supabase/Vercel/GitHub — Claude Code ich nie wykona; pozostałe to sesje Claude Code.

- [x] **5.1** *(panel + Claude Code)* Projekt Supabase „produkcja": nowy projekt w panelu, następnie zastosowanie **wszystkich** migracji z `supabase/migrations/` na czystej bazie. To zarazem test zasady twardej „schemat wyłącznie z migracji" — jeśli schemat nie odtwarza się z repo, najpierw naprawiamy migracje, nie klikamy w panelu. → Użytkownik założył projekt `budzet-app-produkcja` (ref `kblbuqhckvzjqxcjtpbk`, `eu-west-2`) w panelu. `supabase link` + `supabase db push` zastosowały wszystkie 4 migracje bez żadnych poprawek — zasada „schemat tylko z migracji" przeszła test bez zastrzeżeń. Zweryfikowane niezależnie od rejestru migracji: `supabase inspect db table-stats --linked` pokazuje wszystkich 5 tabel (`imports`, `clients`, `revenue_items`, `revenue_months`, `report_archive`), wszystkie puste. CLI przełączone z powrotem na projekt demo (`lcbujqrxmiakvczsplti`) po weryfikacji, żeby nie zostać przypadkiem zalinkowanym do produkcji.
- [x] **5.2** *(panel)* Drugi projekt Vercel z tego samego repozytorium: zmienne środowiskowe wskazujące na Supabase „produkcja", przypięcie do gałęzi `main`, nowy URL nieujawniany publicznie. Konto właściciela w Supabase Auth produkcji (rejestracja samodzielna wyłączona, Site URL ustawiony na produkcyjny URL — wniosek z incydentu utraty konta 15.08). Test logowania. → Nowy projekt Vercel `budzet-app-produkcja` (`https://budzet-app-produkcja.vercel.app`), gałąź `main`. Site URL i wyłączona rejestracja ustawione w Supabase Auth produkcji, konto właściciela założone ręcznie z „Auto Confirm User". Logowanie na produkcyjnym URL działa, baza pusta (potwierdza izolację od demo). Napotkany i rozwiązany problem: `NEXT_PUBLIC_SUPABASE_URL` dodany pierwotnie w Vercelu jako typ **„Secret"** zamiast **„Config"** — zmienne `Secret` są write-only (nie da się ich potem podejrzeć ani przekonwertować na `Config`, trzeba usunąć i dodać od nowa) i nie nadają się dla zmiennych z prefiksem `NEXT_PUBLIC_` (Next.js wbudowuje je do kodu przeglądarki w trakcie builda, więc muszą być plaintext). Objaw w praniu: żądanie logowania leciało na `.../rest/v1/auth/v1/token` zamiast `.../auth/v1/token` (błąd PostgREST `PGRST125`, widoczny dopiero po sprawdzeniu dokładnego Request URL w Network, nie samej treści odpowiedzi) — sam komunikat aplikacji „Nieprawidłowy e-mail lub hasło" jest generyczny dla każdego błędu logowania i nie wskazywał przyczyny. Naprawa: usunięcie i ręczne (nie kopiowane) dodanie zmiennej jako `Config` z wartością wpisaną wprost z ref projektu. Panel Vercela ma też nowy układ (Environment Variables przeniesione pod „Environments", nie „Settings" wprost) — inny niż w dotychczasowej dokumentacji/pamięci.
- [x] **5.3** *(panel + Claude Code)* Gałąź `demo` utworzona z commita wersji ocenionej; przepięcie dotychczasowego projektu Vercel (`budzet-app-sigma.vercel.app`) z `main` na `demo`. Od tej chwili commit na `main` nie zmienia publicznego demo. Aktualizacje `demo` wyłącznie świadome (merge wybranych zmian + osobna weryfikacja). → Gałąź `demo` utworzona z `4a2f680` (ostatni commit sprzed oddania 2026-08-21, tuż po zadaniu 4.6) i wypchnięta na GitHub. W Vercelu branch środowiska Production przełączony z `main` na `demo` (Settings → Environments → Production — nie „Settings → Git", inny układ panelu niż spodziewany). Branch nigdy wcześniej niebudowany w tym projekcie wymagał ręcznego pierwszego deploya (Deployments → „Create Deployment" → wybór gałęzi `demo`) — samo przepięcie ustawienia go nie wyzwoliło. Zweryfikowane: deploy `Ready`, dokładnie commit `4a2f680`, oznaczony `Production`; `budzet-app-sigma.vercel.app` działa bez zmian (logowanie kontem demo, dane syntetyczne). Efekt uboczny do zanotowania: push gałęzi `demo` na GitHub automatycznie wyzwolił dodatkowy podglądowy (preview) deploy w tym samym projekcie Vercel — nieszkodliwy, użytkownik usunął go ręcznie.
- [x] **5.4** Ping podtrzymujący (`SPEC.md` V.49): cron raz dziennie wykonujący trywialne zapytanie do **obu** instancji chmurowych (demo i produkcja). Wariant preferowany: GitHub Actions z zapytaniem REST bezpośrednio do Supabase (zero zmian w kodzie aplikacji — gałąź `demo` zostaje dokładnie na ocenionym commicie). Wariant alternatywny: endpoint `/api/keep-alive` + Vercel cron — wymagałby świadomej aktualizacji `demo`. Weryfikacja skuteczności: obie instancje aktywne po tygodniu bez ręcznej aktywności. → Zrealizowany wariant preferowany: `.github/workflows/keep-alive.yml`, harmonogram `0 4 * * *` (codziennie) + `workflow_dispatch` do testów ręcznych, żyje wyłącznie na `main` — harmonogramy GitHub Actions uruchamiają się z wersji pliku na gałęzi domyślnej, więc `demo` zostaje dokładnie na ocenionym commicie, zero zmian w kodzie aplikacji. Zapytanie idzie kluczem `secret` (nie `publishable`) osobno dla każdej instancji — tabele mają RLS włączone bez żadnych polityk (`CLAUDE.md` pkt 4), więc tylko `secret` faktycznie przechodzi przez bazę i daje realny SELECT, nie samo odbite przez RLS 401. 4 sekrety w GitHub Actions (`SUPABASE_DEMO_URL`, `SUPABASE_DEMO_SECRET_KEY`, `SUPABASE_PROD_URL`, `SUPABASE_PROD_SECRET_KEY`) — URL-e ustawione przez Claude Code (nie są tajne same w sobie), klucze `secret` ustawione ręcznie przez użytkownika. Ręczne uruchomienie (`workflow_dispatch`) potwierdziło sukces (oba pingi 200) dopiero za drugim razem — pierwsza próba nadpisania kluczy przez interaktywny `read -s` w terminalu przez `!` zawiodła po cichu (pusty sekret, bez błędu), naprawione ustawieniem wartości wprost przez GitHub Web UI (Settings → Secrets and variables → Actions). Weryfikacja skuteczności po tygodniu (oba projekty wciąż aktywne bez ręcznej interwencji) — do potwierdzenia później, nie da się sprawdzić od razu.
- [x] **5.5** *(panel)* Dedykowane konto demo w Supabase Auth **instancji demo** (`SPEC.md` V.48) — publiczne hasło nie może być hasłem właściciela. Test logowania dokładnie tymi danymi, które trafią do prezentacji. → Wykonane przed 5.4, na wyraźną prośbę użytkownika — wolał nie zostawiać swojego prawdziwego adresu e-mail w danych logowania, które mogą trafić szerzej niż tylko do organizatorów. Nowe konto `demo@budzet-app.vibecoding` (adres celowo fikcyjny/nieodbieralny — dopuszczalne, bo tworzenie z panelu z „Auto Confirm User" nigdy nie wysyła prawdziwej wiadomości e-mail) na instancji `budzet-app-demo`, hasło ustalone przez użytkownika. Zalogowano się nim na `budzet-app-sigma.vercel.app`, dane w prezentacji podmienione. Stare konto właściciela (prawdziwy e-mail) usunięte z instancji demo — zostaje wyłącznie na produkcji (`budzet-app-produkcja`, zadanie 5.2).
- [x] **5.6** Lokalny Supabase (`SPEC.md` V.50): instalacja Supabase CLI + Docker, `supabase start`, migracje z repo, seed danymi syntetycznymi (`test-data/dane-syntetyczne-clean.xlsx` przez UI lokalne lub skrypt), `.env.local` przełączony na localhost. Weryfikacja izolacji: import lokalny nie zmienia żadnej instancji chmurowej (sprawdzić liczbę importów w bazie demo przed/po). → **Docker/WSL:** Docker Desktop po instalacji zgłaszał brak środowiska wirtualnego; samo `wsl --install` nie pomogło. Naprawiło dopiero (PowerShell jako administrator): `wsl --install --no-distribution` (włącza oba wymagane składniki Windows — „Windows Subsystem for Linux" i „Virtual Machine Platform" — bez instalowania żadnej dystrybucji Linuksa), restart komputera, potwierdzenie przez `wsl --status` (komunikat o braku wirtualizacji zniknął), ponowne uruchomienie Docker Desktop. **`supabase start`** pobrał obrazy i zaaplikował automatycznie wszystkie 4 migracje z repo bez poprawek (API `http://127.0.0.1:54321`, Studio `http://127.0.0.1:54323`). CLI zostaje zalinkowane z projektem demo (`supabase link`) — to nie ma znaczenia dla `start`, który zawsze uruchamia niezależny lokalny stack; ma znaczenie tylko dla `db push`/`db pull`. **`.env.local`** przełączony na wartości lokalne (URL + publishable/secret key ze `supabase start`); poprzednie wartości demo zachowane w niecommitowanym `.env.demo.local.bak` (wzorzec `.env*` w `.gitignore`) do łatwego powrotu. **Konto lokalne:** Auth lokalny ma własną, pustą bazę użytkowników niezależną od demo/produkcji — konto `dev@localhost.test` założone jednorazowo przez REST API GoTrue (`/auth/v1/admin/users` z lokalnym `service_role`, `email_confirm: true`) jako odpowiednik ręcznego „Auto Confirm User" w panelu, którego lokalnie nie ma. **Seed:** import `test-data/dane-syntetyczne-clean.xlsx` wykonany przez rzeczywisty endpoint `/api/import` (ta sama ścieżka kodu co UI) — narzędzie do automatyzacji przeglądarki nie miało dostępu do wgrywania plików z dysku nawet po `/add-dir` (osobny sandbox), więc plik tymczasowo podłożono w `public/`, `fetch` w konsoli przeglądarki (ta sama zalogowana karta, ciasteczka sesji dołączone automatycznie) pobrał go i wysłał `POST /api/import` jako `FormData`; plik tymczasowy usunięty z `public/` zaraz po. Wynik: 1127 wersów, zakres 2024/01–2027/12, zestawienia na `/reports` liczą się poprawnie (204 klientów zapłaciło, 476 771,90 zł przychodów za 2026/08). **Weryfikacja izolacji:** lokalny stack i instancja demo mają rozłączne URL-e i klucze (127.0.0.1 vs. `lcbujqrxmiakvczsplti.supabase.co`), więc odseparowanie jest strukturalne, nie tylko obserwowane. Odczyt REST `imports` na demo pokazał **2** wiersze zamiast 1 udokumentowanego w 4.2a (2026-08-19) — wyjaśnione: użytkownik świadomie wgrał na demo dodatkowy błędny import (obok poprawnego), żeby w prezentacji pracy zaliczeniowej pokazać obsługę obu przypadków; kod loguje do `imports` również importy zakończone `blad` (`src/app/api/import/route.ts`), więc oba wiersze są oczekiwane.
- [x] **5.7** Aktualizacja dokumentów: `README.md` (uwaga „jedna współdzielona instancja" nieaktualna; uruchomienie lokalne przez `supabase start`; opis trzech środowisk), nowy `DEMO.md` z procedurą odtwarzania danych demo po dewastacji przez odwiedzających (reimport pliku syntetycznego + rozkład typów słownika jak w 4.2a — `SPEC.md` V.48). → `README.md`: usunięta nieaktualna uwaga o jednej współdzielonej instancji, dodana tabela trzech środowisk (ta sama z `SPEC.md` VI.3), sekcja lokalnego uruchomienia rozszerzona o Docker/WSL2, `supabase start` i ręczne założenie lokalnego konta Auth (odzwierciedla przebieg zadania 5.6). Nowy `DEMO.md`: procedura pełnego resetu (TRUNCATE pięciu tabel + reimport + SQL odtwarzający rozkład typów 50/25/325/100 przez `row_number() over (order by nip)` — dokładne NIP-y w rozkładzie nie mają znaczenia, tylko proporcje, zgodnie z V.48/4.2) oraz szybszy wariant, gdy zepsuł się tylko słownik.
- [x] **5.8** *(właściciel)* Przegląd prezentacji przed publikacją: nic ponad link i dane logowania **demo**, zero zrzutów z danymi rzeczywistymi (wniosek z incydentu 19.08). Dopiero po tym przeglądzie: zgoda dla organizatora na publikację i podmiana danych logowania w prezentacji na konto z 5.5. → Wykonane przez właściciela. **[2026-08-28] Kurs zakończony i oceniony.** Prezentacja (link + dane logowania demo) będzie publikowana **trwale** na stronie organizatora jako jeden z przykładów pozytywnych — nie jednorazowo do oceny. To potwierdza zasadność rozdziału środowisk z tego etapu: dalsza praca (nowe funkcje, dane rzeczywiste) toczy się wyłącznie na instancji produkcyjnej, nigdy nie dotykając tego, co zostaje trwale wystawione publicznie pod linkiem demo.

**Kryterium ukończenia — spełnione.** Trzy środowiska działają niezależnie — publiczny link pokazuje wersję ocenioną z danymi syntetycznymi i loguje kontem demo; nowy URL produkcyjny loguje kontem właściciela na pustej (zmigrowanej) bazie; `npm run dev` działa na bazie lokalnej (`supabase start`, zadanie 5.6). Commit na `main` nie zmienia demo. Organizator dostał zgodę i właściwe dane logowania.

**Etap 5 zamknięty (2026-08-28).**

---

## Etap 6 — Produkcja: dane rzeczywiste [NOWY 2026-08-27]

Zależy od Etapu 5 w całości. Pierwszy w historii projektu kontakt danych rzeczywistych z instancją, do której nikt poza właścicielem nie ma dostępu — okno czasowe z `SPEC.md` V.41 traci rację bytu.

- [x] **6.1** Import realnego pliku sprzedażowego na produkcję (przez UI) + backtest zestawień względem budżetu na co najmniej 2 miesiącach. Rozbieżności wyjaśniane jak dotychczas: błąd aplikacji albo błąd źródła. → Wykonane przez właściciela: import zrobiony, zestawienia sprawdzone względem budżetu i zestawień dla funduszy — zgodne.
- [x] **6.2** Kategoryzacja słownika klientów (wszyscy wejdą jako „nieokreślony" — narzędzia z zadania 3.7: wyszukiwanie, sortowanie, operacje zbiorcze). Weryfikacja zestawienia 16 po kategoryzacji. → Wykonane przez właściciela: oznaczenia banków i SKOK-ów wprowadzone.
- [x] **6.3** ~~Zrzut tabeli `clients` do pliku lokalnego po kategoryzacji~~ (`SPEC.md` V.49 — jedyne dane produkcyjne nieodtwarzalne z pliku źródłowego, obok migawek archiwum). → **Świadomie pominięte (decyzja użytkownika, `SPEC.md` V.51, koryguje V.49):** lista klientów odtwarza się z pliku źródłowego przy każdym imporcie, a ponowne oznaczenie banków/SKOK-ów w UI zajmuje sekundy — koszt zrzutu i utrzymywania procedury przewyższa koszt ewentualnego odtworzenia. Uzasadnienie V.49 dla migawek `report_archive` (jedynych rzeczywiście nieodtwarzalnych danych) pozostaje bez zmian.

**Kryterium ukończenia — spełnione (bez 6.3, świadomie pominiętego, patrz wyżej):** zestawienia na produkcji zgodne z budżetem, słownik skategoryzowany.

**Etap 6 zamknięty (2026-08-28).**

**Dalsza kolejka po Etapie 6 (bez statusu zadania — każda pozycja wymaga osobnej decyzji/wyceny):** P2 z `DESIGN_SPEC.md` (nawigacja między 16 raportami), dashboard startowy, pytania otwarte P3/P4/P7 poniżej, zapis pełnej listy błędów walidacji w `imports` (notatka przy 1.6c), obserwacja wydajności `/data`.

---

## Pytania otwarte

Do rozstrzygnięcia w trakcie; każde rozstrzygnięcie dopisujemy do rejestru decyzji w `SPEC.md`.

- [x] **P1** Czy zestawienia mają być liczone na żądanie, czy zapisywane do archiwum przy imporcie? → **Rozstrzygnięte:** liczone na żądanie (już tak działa w 3.3); archiwum (4.1) to osobna, ręczna migawka „co zostało wysłane i kiedy", nie mechanizm ponownego liczenia. Przeglądanie archiwum poza zakresem tej wersji.
- [x] **P2** Czy pierwszy wers pliku to zawsze nagłówek z nazwami miesięcy w formacie rozpoznawalnym maszynowo? Jeśli nie — zakres miesięcy trzeba wykrywać po pozycji kolumny J = styczeń 2024 i liczyć w przód. → **Rozstrzygnięte:** plik importowy ma zawsze dokładnie jeden wiersz nagłówkowy; zakres miesięcy wykrywany wyłącznie pozycyjnie (patrz `SPEC.md` V.23).
- [ ] **P3** Co z klientem, który zmienił NIP (przekształcenie spółki)? Dziś: dwaj różni klienci. Czy to akceptowalne?
- [ ] **P4** Czy w zestawieniu 13 interesują Cię tylko wygaśnięcia w oknie „bieżący miesiąc + 3", czy dowolny miesiąc historyczny do analizy?
- [x] **P5** Czy eksport `.xlsx` ma odwzorowywać układ dotychczasowych sprawozdań dla funduszy, czy wystarczy surowa tabela? → **Rozstrzygnięte:** surowa tabela — sprawozdań referencyjnych jest kilka, każde z innym układem, a część danych i tak przekazywana jest w komentarzu tekstowym poza tabelą (patrz `SPEC.md` V.39).
- [x] **P6** Jak backtestować na danych rzeczywistych same EKRANY (nie tylko czyste funkcje w `lib/`), bez ryzyka powtórki incydentu 16.08? → **Rozstrzygnięte (SPEC.md V.41):** tymczasowe okno na współdzielonej instancji, tylko do czasu założenia konta dla organizatorów (4.3, wciąż odłożone) — `local-data/Sprzedaz.xlsx` (bez 3 klientów NDA) importowany do porównania zestawień 13–15 z budżetem. Przed oddaniem projektu: `TRUNCATE` **pięciu** tabel (w tym `report_archive`) + reimport danych syntetycznych. Dane finalne dla oceniających pozostają wyłącznie syntetyczne — pseudonimizacja odrzucona jako dane finalne (ryzyko odczytania realnych wzorców jako błędu aplikacji).
- [ ] **P7** [NOWE] Zestawienie 13 rozbite w przyszłości na dwa odrębne: (1) „klienci, których pakiet kończy się w miesiącu M" (obejmuje też tych, którzy już przedłużyli — odpowiada pierwotnej definicji z tylko M+1) i (2) obecne, ważniejsze „klienci, którzy wygasli w M i dotychczas nie przedłużyli" (SPEC.md V.43, f). Odłożone — obecne zestawienie 13 już realizuje (2), (1) do rozważenia po zaliczeniu.

---

## Dziennik projektu

Krótkie notatki po każdej sesji: co zrobione, co zaskoczyło, co wymagało poprawki. Materiał na dokumentację z zadania 4.5.

| Dzień | Etap / zadanie | Notatka |
|---|---|---|
| 2026-08-15 | 1.2 | Utrata dostępu do konta właściciela (zgubione hasło, Site URL w Supabase Auth wskazywał na localhost — reset hasła prowadził donikąd). Naprawiono ustawienie Site URL, stare konto usunięto, założono nowe. Przy okazji implementacji walidacji struktury dopisano do SPEC.md (II.5, V.25) reguły doprecyzowane z użytkownikiem (m.in. suma kontrolna NIP, wymóg wartości `1` we fladze). Test ujawnił błąd we własnej regule walidacji VAT UE (akceptowała czyste litery zamiast wymagać cyfr) — poprawiono przed pushem. |
| 2026-08-15 | 1.3 | Osobny moduł (`validateFlagRules.ts`) na typ dokumentu i spójność flaga↔liczba miesięcy — reguła flagi H (SPEC.md V.27) doprecyzowana z użytkownikiem: brak ograniczenia liczby miesięcy, w przeciwieństwie do F/G (≥2) i I (dokładnie 1). Wynik obu walidatorów (1.2 + 1.3) połączony w jeden raport błędów w `/api/import`, zgodnie z zasadą "wszystkie błędy naraz" (II.3.i). Przy tej samej edycji poprawiono lukę w numeracji rejestru decyzji SPEC.md (przypadkowo pominięty numer 25 z poprzedniej sesji). |
| 2026-08-15 | 1.4 | Najtrudniejsza reguła projektu — okazała się też najbardziej pouczająca. Analiza empiryczna na pliku syntetycznym (zamiast zgadywania) rozstrzygnęła, że flaga I nie może uczestniczyć w łańcuchu ciągłości dostępu (55 rozbieżności przy włączeniu, zero przy wykluczeniu). Ta sama analiza ujawniła realny błąd w `scripts/generate-synthetic-data.ts`: generator liczył przerwę między pakietami od końca ostatniego wersu w ogóle (także incydentalnych), zamiast od końca ostatniego prawdziwego dostępu F/G — 22 wersy w "czystym" pliku miały przez to błędną flagę. Zgodnie z zasadą "test się nie myli, koryguje się kod" naprawiono generator (nie osłabiono reguły walidacji) i przegenerowano oba pliki syntetyczne. Wniosek na przyszłość: golden file wygenerowany przed napisaniem danej reguły walidacji nie gwarantuje, że reguła ta faktycznie go potwierdzi jako czysty — warto to sprawdzać przy każdej nowej regule, nie zakładać z góry. |
| 2026-08-15 | 1.1–1.4 (test na danych rzeczywistych) | Pierwszy test całego pipeline'u walidacji (1.1–1.4) na prawdziwym, zanonimizowanym pliku sprzedażowym (`local-data/Sprzedaz.xlsx`), zamiast wyłącznie na danych syntetycznych — zaplanowany dopiero na backtest w Etapie 2, ale zrobiony wcześniej jako dodatkowa weryfikacja. Wynik: 99 błędów przy pierwszym przebiegu. Część okazała się realnymi błędami w danych budżetu (znalezionymi i poprawionymi przez użytkownika — bez tej analizy trudnymi do wychwycenia ręcznie), część ujawniła trzy błędne założenia biznesowe w `SPEC.md`, skorygowane po kolei i potwierdzone z użytkownikiem: brak wymogu 2+ miesięcy dla flag F/G (V.29), FVZK z flagą G jako dopuszczalne (V.30), oraz zniesienie 12-miesięcznego okna tolerancji dla braku widocznej historii klienta (V.31) — bo realna historia projektu sięga do 2022 roku w innej spółce, skąd dane nigdy nie trafią do pliku importowego. Rozważono i odrzucono przejście na import przyrostowy z pełną historią jako rozwiązanie tego ostatniego problemu — złamałoby fundamentalną zasadę importu całościowego (V.2) przy 14-dniowym terminie; dobry pomysł na rozszerzenie po zaliczeniu. Po tych trzech korektach plik przechodzi walidację w całości. |
| 2026-08-16 | 2.1 | Użytkownik poprosił o sposób sprawdzenia silnika zestawień na realnych danych budżetu **bez** ryzyka powtórzenia incydentu z instancją demo. Ważne ustalenie: „lokalnie" nie oznacza osobnej bazy — jest tylko jedna instancja Supabase, więc nawet import przez `npm run dev` zapisałby dane do tej samej, współdzielonej bazy. Rozwiązanie: tryb `--file=` w `scripts/preview-report.ts` czytający `.xlsx` bezpośrednio z dysku, zero zapytań do Supabase. Przy tej okazji ujawniony (i naprawiony) błąd we własnym procesie: dodanie rozszerzeń `.ts` do importów (wymagane, żeby Node mógł uruchomić skrypt bezpośrednio, bez bundlera) złamało `next build` (tsconfig domyślnie tego zabrania), a ja nie przebudowałem projektu po tej zmianie przed poprzednim commitem (2.1) — złamanie zasady "buduj przed zakończeniem zadania". Naprawione: `allowImportingTsExtensions` w `tsconfig.json` + rozszerzenia `.ts` dodane też w dwóch tranzytywnie importowanych plikach (`validateFlagRules.ts`, `validateFlagContinuity.ts`). Wniosek na przyszłość: po każdej zmianie importów uruchomić `npm run build` od nowa, nie tylko testy jednostkowe — Vitest i `next build` mają inne mechanizmy rozwiązywania modułów. |
| 2026-08-17 | 2.2 (weryfikacja na danych rzeczywistych) | Użytkownik porównał zestawienia 1–11 (skrypt podglądu) z Excelem dla dwóch miesięcy i znalazł rozbieżności. Po analizie: błąd był w pliku budżetu, nie w kodzie — przynajmniej jedna formuła rozbicia miesięcznego odwoływała się do sztywnego wersu (nie względnego), co po skopiowaniu formuł w dół i usunięciu kilku wersów od góry przesunęło odwołanie i zepsuło wynik dla tego wersu. Plik poprawiony (kwoty wklejone jako wartości zamiast formuł). Potwierdza to sens decyzji V.33 (aplikacja czyta kolumny miesięczne wprost, nic nie licząc od siebie) — błąd źródła danych wiernie przeszedł na wynik, zamiast zostać cicho zamaskowany, i został wykryty dzięki wczesnemu porównaniu z realnymi danymi, a nie dopiero przy backteście 2.7. |
| 2026-08-17 | 2.7 (zamknięcie backtestu) | Zamiast jednorazowego backtestu na końcu Etapu 2, weryfikacja z Excelem odbywała się na bieżąco po każdym zadaniu (2.2, 2.3) — 2 miesiące z 2026 roku, `local-data/Sprzedaz.xlsx`. Rozważono trzeci miesiąc z lat wcześniejszych, ale świadomie pominięto: istniejące ręczne zestawienia referencyjne dla zamkniętych lat były liczone na starych, jeszcze niepoprawionych danych budżetu, więc pokazałyby drobne rozbieżności niezwiązane z kodem aplikacji — porównanie miałoby sens dopiero po przeliczeniu tych zestawień referencyjnych na poprawionych danych (~30–40 min pracy wyłącznie do jednorazowego porównania). Koszt uznany za nieproporcjonalny do spodziewanej korzyści (potwierdzenie czegoś, co już wiadomo). Etap 2 zamknięty w zakresie zredukowanym decyzją V.37 (zestawienia 1–12, 16; 13–15 odłożone). |
| 2026-08-17 | 3.2 | Zadanie 3.1 odhaczone bez dodatkowej pracy — ekran uploadu z historią importów i raportem błędów powstał już w 1.7, kryterium Etapu 3 dla niego było spełnione od razu. Nowość: strona `/clients` musiała dostać `export const dynamic = "force-dynamic"` — bez tego `next build` statycznie prerenderował listę klientów w momencie builda (dane zamrożone na zawsze między deployami, bo import nie wywołuje `revalidatePath("/clients")`), co po cichu złamałoby sens zestawienia 16.c („lista nieokreślonych po każdym imporcie"). Wykryte przez czytanie oznaczeń tras w wyjściu `next build` (○ statyczna vs ƒ dynamiczna), nie przez błąd w runtime — warto to sprawdzać przy każdej nowej stronie czytającej z bazy bez własnego sprawdzenia sesji. |
| 2026-08-17 | 3.3/3.4 | Przy 3.3 użytkownik zapytał o backtest ekranu na danych rzeczywistych (`local-data/Sprzedaz.xlsx`). Odpowiedź: nie przez działającą aplikację — nawet lokalnie (`npm run dev`) trafiłoby to do tej samej współdzielonej instancji Supabase pod publicznym linkiem, czyli powtórka incydentu z 16.08. Ustalone z użytkownikiem: matematyka w 3.3 jest niezmieniona względem już zbacktestowanych funkcji (2.2/2.3/2.7), więc backtest pominięty tym razem — ale sposób na bezpieczne testowanie samych EKRANÓW (nie tylko czystych funkcji) na danych rzeczywistych będzie potrzebny przy 13-15 i był potrzebny przy 3.4 (nowy kod eksportu) — dopisane jako P6. Przy 3.4 rozstrzygnięto też P5 (eksport = surowa tabela, nie układ sprawozdań referencyjnych — SPEC.md V.39) po pytaniu zadanym w prozie, zgodnie z ustaloną preferencją użytkownika co do pytań biznesowych. |
| 2026-08-17 | 4.1/4.2 | Przy 4.2 próba uzupełnienia słownika klientów demo jedną pętlą (4 grupy typu, ~400 wierszy) w skrypcie node została zablokowana przez klasyfikator auto mode Claude Code — słusznie, to zapis masowy na współdzielonej instancji. Po zgodzie użytkownika wykonane jako kilka osobnych, pojedynczych operacji `.update().in()` (jedna na typ) — każda przeszła bez blokady. Wniosek na przyszłość: masowe zmiany danych na tej instancji dzielić na osobne, mniejsze operacje, nie jedną pętlę. |
| 2026-08-17 | P6 (przed 2.4) | Przed rozpoczęciem zestawień 13–15 użytkownik zaproponował rozstrzygnięcie P6: tymczasowe okno testów na danych rzeczywistych (`local-data/Sprzedaz.xlsx`, już bez 3 klientów NDA) na współdzielonej instancji, zamykane `TRUNCATE`-em przed założeniem konta dla organizatorów (4.3). Po dyskusji o ryzykach (odbiorcy niebędący pracownikami spółki, poufność 3 klientów, nowa tabela `report_archive` od 4.1, koszt odwrócenia po 16.08) i propozycji pseudonimizacji jako danych finalnych — **odrzuconej przez użytkownika**: realne wzorce liczbowe pod fikcyjnymi nazwami mogłyby zostać ocenione jako niedokładność aplikacji, nie właściwość źródła danych. Dane finalne dla oceniających zostają wyłącznie syntetyczne (bez zmian względem V.18). Decyzja w `SPEC.md` V.41, nowe zadanie 4.2a w checkliście (musi nastąpić przed 4.3). |
| 2026-08-17 | 2.4 (backtest, w toku) | Zaimplementowano zestawienie 13 (`buildExpiringContractsReport`, seria F/G/H/FKS bez I — potwierdzone z użytkownikiem, że wykluczenie I obejmuje też wartość i faktury, nie tylko test kwalifikujący) i podłączono do `/reports`, eksportu `.xlsx` i archiwum, z regułą horyzontu (`isWithinExpiringHorizon`). Backtest dla marca 2026 (`local-data/Sprzedaz.xlsx`, zaimportowany na współdzieloną instancję za zgodą użytkownika, zgodnie z V.41) w pełni zgodny na trzech poziomach: podgląd z pliku, podgląd z bazy, ekran `/reports` — identyczne 10 klientów, kwoty, dokumenty. Użytkownik sprawdził jednak dwa inne miesiące względem budżetu referencyjnego i znalazł rozbieżności — do zbadania rano. Import lokalnego pliku przez UI okazał się niemożliwy do zautomatyzowania (sandbox `file_upload` w Claude in Chrome nie dopuszcza plików spoza katalogów jawnie udostępnionych sesji) — użytkownik wgrał plik samodzielnie przez własną przeglądarkę, co było najprostszym rozwiązaniem. |
| 2026-08-18 | 2.4 (poprawka definicji po backteście) | Rozbieżności z maja 2026 (Salumanus, Wschodni Bank Spółdzielczy w Chełmie) okazały się ani błędem aplikacji, ani błędem pliku/formuły — użytkownik wyjaśnił, że rozliczenie międzyokresowe opiera się na dacie faktycznego dostępu z faktury, nie na dacie jej wystawienia, i klienci czasem przedłużają z opóźnieniem (po utracie dostępu) albo ze świadomą przerwą. Realny problem: definicja zestawienia 13 sprawdzała tylko M+1, więc klient z przerwą dłuższą niż 1 miesiąc, który JUŻ przedłużył (widoczne w danych), wciąż był błędnie wykazywany jako "nieprzedłużony". Użytkownik zauważył też, że to sygnalizuje potrzebę dwóch odrębnych zestawień w przyszłości (P7) — obecne poprawione tak, żeby definiowało ważniejszy z tych dwóch przypadków: "wygasło i dotychczas nie przedłużyli", sprawdzając brak przychodu w KAŻDYM miesiącu po M (nie tylko M+1), aż do końca dostępnych danych z ostatniego importu. Zmiana w `buildExpiringContractsReport.ts`, decyzja `SPEC.md` V.43. Po zamknięciu backtestu 13 (pełna zgodność na innych miesiącach) przejście do 2.5. |
| 2026-08-18 | 3.6–3.9 (planowanie) | Użytkownik dostarczył `DESIGN_SPEC.md` (usprawnienia UX/wizualne wypracowane w osobnej rozmowie czatowej). Analiza priorytetów zweryfikowana względem faktycznego stanu kodu (nie tylko dokumentu) — okazało się, że `ClientReportTable` już jest komponentem współdzielonym między raportami 12–15, więc dodanie wyszukiwania/sortowania jest tańsze niż szacowano w dokumencie (jeden plik). Ustalony z użytkownikiem harmonogram: okno do 2026-08-21 14:00 na zadania 3.6–3.9, z rezerwą 48h przed terminem (2026-08-23 23:59) na domknięcie 4.2a/4.3/4.6/4.7 — te ostatnie mają pierwszeństwo, jeśli czasu zabraknie. Nawigacja zakładkowa i accordion (P2 w `DESIGN_SPEC.md`) świadomie odłożone poza ten sprint — struktura komponentów/routingu, zbyt duże ryzyko przy napiętym terminie. Kolejność realizacji: 3.6 (tokeny) → 3.7 (słownik klientów, najwyższy zysk funkcjonalny) → 3.8 (selektor + tabela raportów) → 3.9 (opcjonalnie, popover dokumentów). Czas szacowany do porównania z rzeczywistym po każdym zadaniu. |
| 2026-08-18 | 2.5 | Zanim napisano kod, ustalono z użytkownikiem z góry (na podstawie lekcji z 13), że kwalifikacja klienta jako "nowego"/"przedłużającego od M" powinna opierać się na fladze wersu (najwcześniejszy miesiąc z przychodem w tym wersie), nie na agregacie M−1/M/M+1 — unika to identycznej klasy błędu bez potrzeby najpierw go popełnić i naprawiać. Wartość z M+1 (potwierdzone z użytkownikiem, symetrycznie do 13's M−1). Reguła horyzontu zmieniona zgodnie z tym (wymaga tylko M+1, nie M−1 — decyzja `SPEC.md` V.44, koryguje III.B.14.c/15.c). Backtest na marcu i maju 2026 bez rozbieżności między plikiem/bazą/ekranem. Jedyny nietypowy przypadek do wyjaśnienia z użytkownikiem: klient z cykliczną comiesięczną fakturą (MiŚOT S.A., osobny wers G co miesiąc, każdy pokrywający tylko swój miesiąc) pojawiał się w zestawieniu 15 każdego miesiąca — wyglądało to na szum, ale użytkownik potwierdził, że to zgodne ze stanem faktycznym (taki klient faktycznie decyduje o przedłużeniu co miesiąc), więc kod pozostał bez zmian. Etap 2 zamknięty w pełnym zakresie (1–16), nie tylko zredukowanym z V.37. |
| 2026-08-19 | 3.5, 3.9, drobne poprawki UI | Zamknięto 3.5 (a/b/c: wspólny `Nav`, `loading.tsx`, `error.tsx`) i opcjonalne 3.9 (popover dokumentów, `<details>/<summary>`) — obie znacznie szybciej niż szacowano. Po zamknięciu całego zakresu `DESIGN_SPEC.md` użytkownik przeglądał `/reports` na danych rzeczywistych (`local-data/Sprzedaz.xlsx`, wciąż w oknie P6/V.41) i zgłaszał drobne poprawki UI pojedynczo: format kwot po polsku (spacja tysięcy, przecinek — nowa funkcja `formatZl` w `app/reports`, celowo osobna od `formatGroszeAsDecimal`, która zasila zapis do bazy i eksport .xlsx i nie mogła się zmienić), stała szerokość kolumn kwot (`table-fixed` + `w-36`, mieści 9 999 999,99 zł) z wyśrodkowanymi nagłówkami trzech ostatnich kolumn w `ClientReportTable`, korekta szerokości NIP (`w-32`, 12 znaków) i Dokumenty (`w-40`, mieści `TYP/rrrr/mm/nnnn`) po tym, jak pierwsza wersja (kolumny NIP/Nazwa/Dokumenty równo dzielące resztę szerokości) wypadła gorzej niż poprzedni układ, poprawna odmiana liczebnika przy liczbie dokumentów (2–4 "dokumenty" vs 5+ "dokumentów", z wyjątkiem 12–14), i sumy kolumn kwot dla przefiltrowanego zbioru wyrównane nad tymi kolumnami mimo że wizualnie siedzą w tym samym wersie co pole wyszukiwania (poza tabelą) — przez dopasowanie szerokości boksów i pusty spacer zamiast osobnego wiersza w `thead`; zastrzeżenie: przy listach na tyle długich, że tabela się przewija, pasek przewijania może przesunąć wyrównanie o ok. 15px. Użytkownik zauważył też, że rzeczywisty czas realizacji 3.6–3.9 był wyraźnie niższy od szacunków z 18.08 (np. 3.7: ~2.5h szacowane → ~1.5h, 3.9: ~0.5–1h → ~15 min) — harmonogram zaktualizowany: okno na dalsze poprawki wydłużone do końca 2026-08-20, bufor na 4.2a/4.3/4.6/4.7 powiększony do 2026-08-21–23 (z 48h do ok. 3 dni). |
| 2026-08-19 | Zestawienie 16 (rozszerzenie) | Użytkownik poprosił o zamianę zestawienia 16 (dotąd sama liczba w tabeli 1–11) na pełną tabelę klientów analogiczną do 12–15. Nowa funkcja `filterBanksAndSkoks` (`lib/reports/`) zwraca listę zamiast liczby — ta sama reguła kwalifikacji co dotychczasowe `countBanksAndSkoks`. Świadomie ograniczono zakres do ekranu `/reports`: `countBanksAndSkoks` zostaje bez zmian i nadal zasila liczbę w archiwum (`report_archive`) i eksporcie `.xlsx`, żeby nie dotykać już przetestowanych/nieproszonych o zmianę ścieżek. Decyzja `SPEC.md` V.45, koryguje III.B.16.a. |
| 2026-08-19 | Zestawienie 16 (rozszerzenie na archiwum/eksport) | Po weryfikacji listy z poprzedniego kroku na kilku miesiącach względem budżetu, użytkownik poprosił o rozszerzenie zmiany na archiwum i eksport, skoro nie niesie to ryzyka. `countBanksAndSkoks` usunięte (razem z testem, w pełni nieużywane) na rzecz `filterBanksAndSkoks`: `report_archive.payload` ma teraz `banksAndSkoksClients` zamiast `summary.banksAndSkoks`; eksport `.xlsx` ma osobny arkusz „Zestawienie 16" (analogiczny do „Zestawienie 12"), wiersz „16." zniknął z arkusza „Zestawienia 1-11" (przemianowanego z „Zestawienia 1-11, 16"). Przy okazji poprawiony `scripts/preview-report.ts` (ten sam import). Decyzja `SPEC.md` V.46, rozszerza V.45. |
| 2026-08-19 | Nowa zakładka "Dane" | Użytkownik poprosił o dwie nowe zakładki nawigacyjne: "Dane" (podgląd danych źródłowych, między "Import" i "Zestawienia") oraz "Archiwum" (między "Zestawienia" i "Słownik klientów", odłożone na kolejny krok). Oceniono ryzyko: obie wyłącznie do odczytu, nie dotykają logiki liczenia/walidacji - "Dane" prostsza (brak historycznych niezgodności formatu), "Archiwum" trudniejsza (payload `report_archive` zmienia kształt w czasie wraz z decyzjami SPEC.md, patrz V.46 tego samego dnia - wymaga defensywnej obsługi starszych migawek). Zrealizowano "Dane": nowa strona `/data`, nowa czysta funkcja `buildSourceDataRows` (`lib/data-preview/`, z testami) odtwarzająca układ arkusza importu (SPEC.md II.2: Lp/Nazwa/NIP/Numer dokumentu/Wartość netto/F/G/H/I/miesiące) z aktywnych `revenue_items`+`revenue_months`, z zerami tam, gdzie w bazie nie ma wpisu (miesiące zerowe nie są zapisywane, SPEC.md V.33). Tabela szeroka (przewijanie poziome i pionowe) - przy obecnym zakresie miesięcy i ~1100-1200 aktywnych wersach warto sprawdzić wydajność renderowania na produkcji. |
| 2026-08-19 | "Dane" - blokowanie kolumn i długie polowanie na przyczynę rozjazdu szerokości | Po pochwale wydajności użytkownik poprosił o drobne poprawki (skrócenie opisu, węższa kolumna Nazwa, etykiety flag N/P/D/I zamiast F/G/H/I - wyłącznie w tej tabeli, bez zmian w danych, sumy nad kolumnami z przychodami) - zrealizowane szybko. Poproszony też o zablokowanie (sticky) kolumn Lp/Nazwa/NIP przy przewijaniu w poziomie i podwojenie szerokości Nazwy - tu zaczęła się długa diagnoza: pierwsza wersja (`position: sticky` bez `table-layout: fixed`) dawała rozjazd między pierwszym renderem a stanem po scrollu. Przejście na `table-fixed` z jawną szerokością każdej kolumny nie pomogło - dopiero zrzuty ekranu i wartości z `getComputedStyle` w DevTools (dostarczone przez użytkownika, kluczowe dla znalezienia przyczyny) ujawniły prawdziwy powód: tabela bez własnej szerokości i tak rozciąga się do szerokości kontenera (karty), a nadmiar miejsca jest rozdzielany proporcjonalnie na wszystkie kolumny nawet pod `table-layout: fixed` (Nazwa: 535,547px zamiast 360px). Próba naprawy przez `w-max` (`width: max-content`) **nie zadziałała wcale** (identyczna wartość przed i po - potwierdzone ponownie w DevTools) - `max-content` na tabeli z fixed layout nie zachowuje się tak, jak można by oczekiwać. Ostateczna naprawa: policzenie dokładnej sumy zadeklarowanych szerokości kolumn (zależnej od liczby miesięcy) i ustawienie jej wprost jako `width` w stylu inline na `<table>` - zero niejednoznaczności, zweryfikowane: `getComputedStyle` pokazuje dokładnie 360px. Wniosek na przyszłość: przy dziwnych, uporczywych rozjazdach layoutu żądać konkretnych wartości z `getComputedStyle`/DevTools zamiast zgadywać kolejne poprawki CSS "na wyczucie" - oszczędza to rundy prób i błędów. |
| 2026-08-19 | Incydent: zrzuty ekranu z danymi produkcyjnymi trafiły do publicznego repo | Przy commitowaniu poprawki `w-max` użyto `git add -A` bez przejrzenia listy plików, co wciągnęło i wypchnęło na GitHub folder `zrzuty/` z prawdziwymi danymi klientów (nazwy, NIP-y, kwoty) z okna testowego P6/V.41 - złamanie własnej zasady sprawdzania stage'a przed commitem. Repozytorium było w tym momencie publiczne. Naprawa (za zgodą użytkownika): (1) repo ustawione na prywatne przez użytkownika - natychmiastowe zatrzymanie ekspozycji; (2) `zrzuty/` dodane do `.gitignore`; (3) `git filter-branch --index-filter` usunął folder ze wszystkich 87 commitów historii, `git gc --prune=now` wyczyścił stare obiekty, `git push --force` nadpisał zdalną historię. Użytkownik potwierdził, że dane w zrzutach nie są poufne (klienci jawni) - ryzyko ostatecznie niskie, ale procedura (private → .gitignore → filter-branch → force push, za wyraźną zgodą na krok destrukcyjny) warta zapamiętania na przyszłość. Skutek uboczny: kolejny push zablokowany przez Vercela ("commit email nie pasuje do konta GitHub") - typowe dla Hobby po zmianach w repo/koncie, naprawione weryfikacją e-maila na właściwym koncie GitHub (`TomaszStefanowicz`) i wymuszeniem nowego deploymentu (pusty commit) zamiast samego "Redeploy" (który ponawiał tylko już zablokowaną wersję). Wniosek: **zawsze przeglądać `git status` po `git add -A` przed commitem**, szczególnie gdy w toku sesji powstały nowe, niekontrolowane katalogi (np. do debugowania). |
| 2026-08-19 | Zakładka "Archiwum" (4.1a) | Zrealizowano drugą z dwóch zaplanowanych zakładek (po "Dane"). Widok identyczny z `/reports` osiągnięty przez wydzielenie współdzielonych komponentów (`SummaryTable` wyekstrahowany z `reports/page.tsx`, reużyte `ClientReportTable`/`ReportSection`) zamiast duplikowania markupu. Zamiast przycisku "Zapisz do archiwum" czerwony napis "ARCHIWUM", zamiast selektora roku/miesiąca rozwijana lista migawek (`ArchiveSelect`, nawigacja przez `?id=`). Świadomie pominięty przycisk eksportu `.xlsx` (działałby na bieżących, nie zarchiwizowanych danych - mylące w kontekście przeglądania historii). Nowa funkcja `archivedPayload.ts` (z testami) konwertuje złotówki z `payload` z powrotem na grosze i defensywnie obsługuje różne kształty migawek zapisanych w różnym czasie (przed/po V.45/V.46, sekcje 13-15 obecne tylko gdy miesiąc był w horyzoncie w chwili archiwizacji) - brakujące sekcje pokazują "niedostępne w tej migawce" zamiast się wysypać. |
| 2026-08-27 | Planowanie Etapu 5 (bez kodu) | Nowa okoliczność: organizator kursu opublikuje na swojej stronie finalną prezentację z linkiem do aplikacji i danymi logowania — demo zmienia status na trwale publiczne, co wymusza wykonanie rozdziału środowisk z `SPEC.md` VI.3 **przed** zgodą na publikację. Rozstrzygnięcia z sesji planistycznej (Claude konwersacyjny, przed implementacją): konto demo dedykowane, z pełnymi uprawnieniami wraz z importem + procedura odtwarzania danych syntetycznych (`SPEC.md` V.48); ping podtrzymujący raz dziennie dla obu instancji zamiast planu płatnego, z zastrzeżeniem zerowych backupów planu darmowego i zrzutem `clients` po kategoryzacji (`SPEC.md` V.49); środowisko dev na lokalnym Supabase przez CLI/Docker — odrzucone współdzielenie bazy demo (migracje pod zamrożonym kodem `demo`) i trzeci projekt chmurowy (limit 2 aktywnych projektów planu darmowego) (`SPEC.md` V.50). Zaktualizowane: `SPEC.md` VI.2/VI.3/VI.7 + decyzje 48–50, `PLAN.md` Etapy 5–6. Dane logowania z rozstrzygnięcia 4.3 (konto właściciela) przestają być przeznaczone do publikacji — zastąpi je konto z zadania 5.5. |
| 2026-08-27 | 5.1 | Użytkownik założył w panelu projekt `budzet-app-produkcja`. `supabase db push` na czystej bazie zastosował wszystkie 4 migracje za jednym razem, bez poprawek — dobry znak, że dyscyplina „schemat tylko z migracji" (zasada twarda nr 5) faktycznie była przestrzegana przez cały projekt, nie tylko deklarowana. `supabase db push` i wcześniej `db pull` na klasyfikatorze auto mode trafiają w kategorię zmian schematu na zdalnej instancji — poproszono o wyraźne potwierdzenie przed wykonaniem, zgodnie z zasadą ostrożności przy nieodwracalnych operacjach. Weryfikacja `supabase inspect db table-stats --linked` (odpytuje bazę bezpośrednio, nie przez rejestr migracji CLI) jako dodatkowy, niezależny dowód — przydatne dla kolejnych zadań Etapu 5, gdzie `supabase db dump`/`db diff --linked` okazały się niedostępne bez Dockera (błąd `LegacyDockerRunError`), a Docker instalujemy dopiero w 5.6. |
| 2026-08-27 | 5.2 | Zadanie czysto panelowe — Claude Code prowadził użytkownika krok po kroku (Vercel + Supabase Auth), bez wykonywania akcji samodzielnie (klucze API świadomie nie pobrane przez CLI mimo dostępnej komendy `supabase projects api-keys` — zablokowane przez klasyfikator auto mode jako ryzyko wycieku sekretu do transkryptu, słusznie). Największa przeszkoda: `NEXT_PUBLIC_SUPABASE_URL` dodany jako typ „Secret" w Vercelu zamiast „Config" — write-only, więc nie dało się zdiagnozować wprost, dopiero po odtworzeniu zmiennej od nowa i porównaniu Request URL w Network. Wniosek na przyszłość: przy jakichkolwiek zmiennych `NEXT_PUBLIC_*` w Vercelu pilnować typu „Config" już przy pierwszym dodawaniu — „Secret" nie da się potem naprawić inaczej niż usuwając i dodając od nowa. Panel Vercela zmienił układ menu (Environment Variables pod „Environments") — kolejny dowód na to, że interfejsy narzędzi third-party bywają aktualizowane bez ostrzeżenia, więc przy prowadzeniu użytkownika krok po kroku lepiej pytać co widzi na ekranie, niż zakładać znajomy układ z dokumentacji/pamięci. |
| 2026-08-27 | 5.3 | Gałąź `demo` utworzona i wypchnięta lokalnie przez Claude Code (`git branch`/`git push` — operacja addytywna, nie dotyka `main`, wykonana bez dodatkowego pytania o zgodę, zgodnie z zasadą dopuszczającą nieryzykowne, odwracalne akcje). Przepięcie „Production Branch" w Vercelu okazało się kolejnym miejscem z przebudowanym panelem (tym razem pod Settings → Environments, nie Settings → Git) — ten sam wzorzec co przy zmiennych środowiskowych w 5.2, drugi raz w tej samej sesji. Nowa nauka: przełączenie gałęzi Production dla projektu, w którym ta gałąź nigdy wcześniej nie była budowana, nie wyzwala automatycznego deploya — trzeba go odpalić ręcznie („Create Deployment" z wyborem gałęzi). Etap 5 dotąd (5.1–5.3) przebiega bez żadnej niespodzianki w samym kodzie/migracjach — całe tarcie jest po stronie zmieniających się interfejsów Vercela i Supabase, nie logiki aplikacji. |
| 2026-08-27 | 5.5 (przed 5.4) | Użytkownik poprosił o wykonanie 5.5 poza kolejnością, tuż po 5.3 — zanim dane logowania do demo trafią do szerszej dystrybucji niż tylko organizatorzy kursu, wolał podmienić realny adres e-mail na fikcyjny. Zrealizowane bez niespodzianek (samo tworzenie/usuwanie kont w Supabase Auth działało zgodnie z oczekiwaniami, w przeciwieństwie do przełączania paneli Vercela w 5.2/5.3). Stare konto właściciela usunięte z instancji demo od razu po potwierdzeniu działania nowego — decyzja użytkownika, nie tylko dodanie konta obok istniejącego. |
| 2026-08-27 | 5.4 | Workflow zaprojektowany do użycia klucza `secret` zamiast `publishable` świadomie, po sprawdzeniu migracji (RLS włączone bez polityk na wszystkich tabelach) — zapytanie kluczem publicznym dostałoby 401 z API gateway, niepewne czy to w ogóle liczy się jako „aktywność" resetująca pauzę Supabase; klucz `secret` daje pewność, bo faktycznie wykonuje SELECT w Postgresie. Praktyczna nauka o narzędziach: próba przekazania sekretu przez `gh secret set` w trybie interaktywnym uruchomionym przez `!` (prefiks do wykonywania poleceń w sesji użytkownika) zawiodła dwukrotnie po cichu — ani prompt `gh` bez `--body`, ani `read -s` nie przechwyciły wpisywanej wartości (pusty sekret bez żadnego błędu w stdout/stderr), co sugeruje, że ten kanał nie obsługuje w pełni interaktywnych promptów terminala mimo że we wcześniejszej sesji (5.1, `supabase link -p`) zadziałał bez zarzutu. Ostatecznie zadziałało tylko wklejenie wartości bezpośrednio w GitHub Web UI. Wniosek na przyszłość: przy przekazywaniu sekretów przez `!` nie ufać interaktywnym promptom typu `read -s` bez weryfikacji — sprawdzać efekt (np. `gh secret list` z sygnaturą czasu) zamiast zakładać sukces z braku błędu. |
